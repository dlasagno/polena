//! Type declarations are exported by default.

export type User = {
  /// User name.
  /// @type {string}
  name: string,
};

/// Function declarations are exported by default.
export fn greeting(user: User): string {
  "Hello ${user.name}"
}
